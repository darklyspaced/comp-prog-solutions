fn main() {
    let lines = include_str!("input.txt").lines().collect::<Vec<&str>>();
    let groups = lines.split()

    for line in input.lines() {
        println!("Here is your line: {}", line)
    }
}
